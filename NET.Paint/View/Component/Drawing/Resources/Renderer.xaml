    <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shape="clr-namespace:NET.Paint.Drawing.Model.Shape;assembly=NET.Paint.Drawing" xmlns:converters="clr-namespace:NET.Paint.View.Component.Drawing.Converters">

    <DataTemplate DataType="{x:Type shape:XPolyline}">
        <DataTemplate.Resources>
            <converters:PointCollectionConverter x:Key="PointCollectionConverter"/>
        </DataTemplate.Resources>
        <Polyline Points="{Binding Points, Converter={StaticResource PointCollectionConverter}}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" StrokeDashArray="{Binding StrokeStyle.DashArray}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XLine}">
        <Line X1="{Binding Start.X}" Y1="{Binding Start.Y}" X2="{Binding End.X}" Y2="{Binding End.Y}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" StrokeDashArray="{Binding StrokeStyle.DashArray}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XCurve}">
        <Path Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Path.Data>
                <PathGeometry>
                    <PathFigure StartPoint="{Binding Start}">
                        <QuadraticBezierSegment Point1="{Binding Ctrl1}" Point2="{Binding End}" IsStroked="True" />
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
        </Path>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XBezier}">
        <Path Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Path.Data>
                <PathGeometry>
                    <PathFigure StartPoint="{Binding Start}">
                        <BezierSegment Point1="{Binding Ctrl1}"  Point2="{Binding Ctrl2}"  Point3="{Binding End}" IsStroked="True" />
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
        </Path>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XCircle}">
        <Ellipse Width="{Binding Width}" Height="{Binding Height}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" Fill="{Binding FillBrush}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Ellipse.RenderTransform>
                <TranslateTransform X="{Binding Location.X}" Y="{Binding Location.Y}"/>
            </Ellipse.RenderTransform>
        </Ellipse>
    </DataTemplate>


    <DataTemplate DataType="{x:Type shape:XEllipse}">
        <Ellipse Width="{Binding Width}" Height="{Binding Height}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" Fill="{Binding FillBrush}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Ellipse.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding Location.X}" Y="{Binding Location.Y}"/>
                    <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
                </TransformGroup>
            </Ellipse.RenderTransform>
        </Ellipse>
    </DataTemplate>


    <DataTemplate DataType="{x:Type shape:XSquare}">
        <Rectangle Width="{Binding Width}" Height="{Binding Height}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" Fill="{Binding FillBrush}" RadiusX="{Binding Radius}" RadiusY="{Binding Radius}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Rectangle.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding Location.X}" Y="{Binding Location.Y}"/>
                    <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
                </TransformGroup>
            </Rectangle.RenderTransform>
        </Rectangle>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XRectangle}">
        <Rectangle Width="{Binding Width}" Height="{Binding Height}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" Fill="{Binding FillBrush}" RadiusX="{Binding Radius}" RadiusY="{Binding Radius}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Rectangle.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding Location.X}" Y="{Binding Location.Y}"/>
                    <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
                </TransformGroup>
            </Rectangle.RenderTransform>
        </Rectangle>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XCloud}">
        <DataTemplate.Resources>
            <converters:SmoothCurveConverter x:Key="SmoothCurveConverter"/>
        </DataTemplate.Resources>
        <Path Fill="{Binding FillBrush}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Path.Data>
                <PathGeometry>
                    <PathFigure StartPoint="{Binding Points[0]}" IsClosed="True">
                        <PolyBezierSegment Points="{Binding Points, Converter={StaticResource SmoothCurveConverter}}" />
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
            <Path.RenderTransform>
                <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
            </Path.RenderTransform>
        </Path>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XPolygon}">
        <DataTemplate.Resources>
            <converters:PointCollectionConverter x:Key="PointCollectionConverter"/>
        </DataTemplate.Resources>
        <Polygon Points="{Binding Points, Converter={StaticResource PointCollectionConverter}}" Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" Fill="{Binding FillBrush}" StrokeDashArray="{Binding StrokeStyle.DashArray}">
            <Polygon.RenderTransform>
                <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
            </Polygon.RenderTransform>
        </Polygon>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XText}">
        <DataTemplate.Resources>
            <converters:TextBoldConverter x:Key="BoolToFontWeightConverter"/>
            <converters:TextItalicConverter x:Key="BoolToFontStyleConverter"/>
            <converters:TextDecorationsConverter x:Key="TextDecorationsConverter"/>
        </DataTemplate.Resources>
        <TextBlock Text="{Binding Text}" FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}" Foreground="{Binding TextColor}" FontWeight="{Binding IsBold, Converter={StaticResource BoolToFontWeightConverter}}" FontStyle="{Binding IsItalic, Converter={StaticResource BoolToFontStyleConverter}}">
            <TextBlock.TextDecorations>
                <MultiBinding Converter="{StaticResource TextDecorationsConverter}">
                    <Binding Path="IsUnderline"/>
                    <Binding Path="IsStrikethrough"/>
                </MultiBinding>
            </TextBlock.TextDecorations>
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding Location.X}" Y="{Binding Location.Y}"/>
                    <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
    </DataTemplate>

    <DataTemplate DataType="{x:Type shape:XBitmap}">
        <Image Source="{Binding Source}" Width="{Binding Width}" Height="{Binding Height}" Stretch="UniformToFill">
            <Image.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="{Binding Location.X}" Y="{Binding Location.Y}"/>
                    <RotateTransform CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}" Angle="{Binding Rotation}"/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>
    </DataTemplate>

</ResourceDictionary>