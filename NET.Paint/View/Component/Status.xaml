<UserControl x:Class="NET.Paint.View.Component.Status"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NET.Paint.View"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:tools="clr-namespace:NET.Paint.Drawing.Model;assembly=NET.Paint.Drawing"
             xmlns:constant="clr-namespace:NET.Paint.Drawing.Constant;assembly=NET.Paint.Drawing"
             xmlns:service="clr-namespace:NET.Paint.Drawing.Service;assembly=NET.Paint.Drawing"
             xmlns:converter="clr-namespace:NET.Paint.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800" d:Background="White"
             d:DataContext="{d:DesignInstance Type=service:XService, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
            <converter:ToolTypeVisibilityConverter x:Key="ToolTypeVisibilityConverter"/>
    </UserControl.Resources>
    
    <StatusBar Background="Transparent">
        <StatusBarItem Margin="5,0,-1,0">
            <TextBlock FontSize="13">
                    <Bold>Tool:</Bold>
                    <Run Text="{Binding Tools.ActiveTool}" FontWeight="SemiBold"/>
            </TextBlock>
        </StatusBarItem>
        <StatusBarItem Margin="-1,0" Visibility="{Binding Tools.ActiveTool, Converter={StaticResource ToolTypeVisibilityConverter}, ConverterParameter={x:Static constant:XToolType.Polygon}}">
            <TextBlock FontSize="13">
                [<Run Text="{Binding Tools.ActivePolygon}"/>]
            </TextBlock>
        </StatusBarItem>
        <StatusBarItem Content="{Binding Tools.ActiveBitmap}" ContentStringFormat="[{0}]" Visibility="{Binding Tools.ActiveTool, Converter={StaticResource ToolTypeVisibilityConverter}, ConverterParameter={x:Static constant:XToolType.Bitmap}}"/>
        <Separator Margin="5,2"/>
        <StatusBarItem Margin="-1,0">
            <TextBlock FontSize="13" FontWeight="SemiBold">
                    <Bold>Mouse:</Bold>
                    [X: <Run Text="{Binding Tools.MouseLocation.X, StringFormat=0.00}"/>,
                    Y: <Run Text="{Binding Tools.MouseLocation.Y, StringFormat=0.00}"/>]
            </TextBlock>
        </StatusBarItem>
        <Separator Margin="5,2"/>
        <StatusBarItem Margin="-1,0">
            <Grid Width="20" Height="20">
                <xctk:ColorPicker Background="{Binding Tools.StrokeColor, Converter={StaticResource ColorToBrushConverter}}" SelectedColor="{Binding Tools.StrokeColor}" ShowDropDownButton="False" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" BorderThickness="0"/>
                <xctk:ColorPicker Background="{Binding Tools.FillColor, Converter={StaticResource ColorToBrushConverter}}" SelectedColor="{Binding Tools.FillColor}" ShowDropDownButton="False" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" BorderThickness="0" Margin="4"/>
            </Grid>
        </StatusBarItem>

        <StatusBarItem Margin="0,0,5,0" HorizontalAlignment="Right">
            <TextBlock FontSize="13">
                <Bold>Active:</Bold>
                <Run Text="{Binding ActiveImage.Title}" FontWeight="SemiBold"/>
                [<Run Text="{Binding ActiveImage.ActiveLayer.Title}" />]
            </TextBlock>
        </StatusBarItem>
    </StatusBar>
</UserControl>
