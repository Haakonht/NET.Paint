<Window x:Class="NET.Paint.View.Component.Dialog.FileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        d:DesignHeight="250" d:DesignWidth="400" WindowStyle="None">
    <xcad:DockingManager>
        <xcad:LayoutRoot>
            <xcad:LayoutPanel>
                <xcad:LayoutAnchorablePane>
                    <xcad:LayoutAnchorable Title="{Binding Title}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="70"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- TreeView for directories -->
                            <TreeView ItemsSource="{Binding Directories}" Grid.Row="0" Grid.Column="0" SelectedItemChanged="TreeView_SelectedItemChanged"/>

                            <!-- ListView for files -->
                            <ListView ItemsSource="{Binding Files}" Grid.Row="0" Grid.Column="1"/>

                            <!-- File name and extension input -->
                            <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="5"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition Width="150"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Height="25" Text="{Binding FileName}" Grid.Column="1"/>
                                    <ComboBox Width="150" ItemsSource="{Binding FileExtensions}" SelectedItem="{Binding SelectedExtension}"  Grid.Column="3"/>
                                </Grid>
                                <UniformGrid Columns="2" HorizontalAlignment="Right" Grid.Row="2" Grid.Column="2">
                                    <Button Content="OK" Command="{Binding OkCommand}" Width="75"/>
                                    <Button Content="Cancel" Width="75"/>
                                </UniformGrid>
                            </Grid>
                        </Grid>
                    </xcad:LayoutAnchorable>
                </xcad:LayoutAnchorablePane>
            </xcad:LayoutPanel>
        </xcad:LayoutRoot>
    </xcad:DockingManager>
</Window>
