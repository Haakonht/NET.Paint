<UserControl x:Class="NET.Paint.View.Component.Overview.Clipboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:utility="clr-namespace:NET.Paint.Drawing.Model.Utility;assembly=NET.Paint.Drawing"
             xmlns:controls="clr-namespace:NET.Paint.View.Component.Overview.Controls"
             xmlns:structure="clr-namespace:NET.Paint.Drawing.Model.Structure;assembly=NET.Paint.Drawing"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="200"
             d:DataContext="{x:Static utility:XClipboard.Instance}" d:Background="White">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../Resources/ShapeIcons.xaml"/>
                <ResourceDictionary Source="Resources/StretchingTreeView.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <ContextMenu x:Key="ClipboardContextMenu">
                <MenuItem Header="Paste" Click="Paste"/>
                <MenuItem Header="Clear" Click="ClearClipboard"/>
            </ContextMenu>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel LastChildFill="True" DataContext="{Binding Clipboard}" ContextMenu="{StaticResource ClipboardContextMenu}">
        <icons:MaterialIcon Kind="ContentCut" DockPanel.Dock="Right" Margin="0,0,5,0" Visibility="{Binding IsCut, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Data}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}" VerticalAlignment="Center" Margin="5,0,0,0">
                            <ContentControl.Resources>
                                <DataTemplate DataType="{x:Type structure:XLayer}">
                                    <TreeView x:Name="ImageTree" BorderBrush="Transparent" Background="Transparent" ItemContainerStyle="{StaticResource StretchingTreeView}">
                                        <TreeViewItem ItemsSource="{Binding Shapes}">
                                            <TreeViewItem.Header>
                                                <controls:Layer DataContext="{Binding}"/>
                                            </TreeViewItem.Header>
                                            <TreeViewItem.Items>
                                                <DataTemplate DataType="{x:Type structure:XRenderable}">
                                                    <controls:Renderable DataContext="{Binding}"/>
                                                </DataTemplate>
                                            </TreeViewItem.Items>
                                        </TreeViewItem>
                                        <TreeView.Resources>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black" />
                                        </TreeView.Resources>
                                    </TreeView>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type structure:XRenderable}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource ShapeSelector}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Type, Mode=OneWay}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ContentControl.Resources>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
