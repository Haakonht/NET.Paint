<UserControl x:Class="NET.Paint.View.Component.Overview.Control.Renderable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NET.Paint.View.Component.Overview.Control"
             xmlns:constants="clr-namespace:NET.Paint.Drawing.Constant;assembly=NET.Paint.Drawing"
             xmlns:converters="clr-namespace:NET.Paint.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../../Resources/ShapeIcons.xaml"/>
                <ResourceDictionary Source="../../../../Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:ShapeNameConverter x:Key="ShapeNameConverter"/>
            <converters:ValueGreaterThanConverter x:Key="ValueGreaterThanConverter"/>
            <converters:ShapeSelectedConverter x:Key="ShapeSelectedConverter"/>

            <Style x:Key="ActiveHoverStyle" TargetType="TextBlock" BasedOn="{StaticResource HoverStyle}">
                <Style.Triggers>
                    <DataTrigger Value="True">
                        <DataTrigger.Binding>
                            <MultiBinding Converter="{StaticResource ShapeSelectedConverter}">
                                <Binding />
                                <Binding Path="DataContext.Selected" RelativeSource="{RelativeSource AncestorType={x:Type TreeViewItem}, AncestorLevel=3}" />
                            </MultiBinding>
                        </DataTrigger.Binding>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" Margin="0,1,0,0">
        <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource ShapeSelector}" VerticalAlignment="Center" Width="15" Height="15"/>
        <TextBlock Text="{Binding Path=., Converter={StaticResource ShapeNameConverter}, Mode=OneWay}" VerticalAlignment="Center" Margin="5,0,0,0" Style="{StaticResource ActiveHoverStyle}"/>

        <Label Padding="0" Margin="0" Visibility="{Binding Type, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static constants:XToolType.Rectangle}}">
            <TextBlock Text="[Rounded]" VerticalAlignment="Center" Margin="3,0,0,0" Visibility="{Binding Radius, Converter={StaticResource ValueGreaterThanConverter}, ConverterParameter=1}"/>
        </Label>

        <TextBlock Text="{Binding Text, Mode=OneWay, StringFormat=[{0}]}" VerticalAlignment="Center" Margin="3,0,0,0" Visibility="{Binding Type, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter={x:Static constants:XToolType.Text}}"/>
    </StackPanel>
</UserControl>
