<UserControl x:Class="NET.Paint.View.Component.Overview.ProjectTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:shape="clr-namespace:NET.Paint.Drawing.Model.Shape;assembly=NET.Paint.Drawing"
             xmlns:service="clr-namespace:NET.Paint.Drawing.Service;assembly=NET.Paint.Drawing"
             xmlns:selector="clr-namespace:NET.Paint.Selector"
             xmlns:converter="clr-namespace:NET.Paint.Converter"
             xmlns:constants="clr-namespace:NET.Paint.Drawing.Constant;assembly=NET.Paint.Drawing"
             xmlns:structure="clr-namespace:NET.Paint.Drawing.Model.Structure;assembly=NET.Paint.Drawing"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200" d:Background="White"
             d:DataContext="{d:DesignInstance Type=service:XService, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../Resources/ShapeIcons.xaml"/>
                <ResourceDictionary Source="../../../Resources/Controls/StretchingTreeView.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converter:ShapeSelectedConverter x:Key="ShapeSelectedConverter"/>
            <converter:LayerActiveConverter x:Key="LayerActiveConverter"/>
            <converter:ImageActiveConverter x:Key="ImageActiveConverter"/>

            <Style TargetType="TextBlock" x:Key="HoverStyle">
                <Setter Property="FontWeight" Value="Normal"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ActiveHoverStyle" TargetType="TextBlock" BasedOn="{StaticResource HoverStyle}">
                <Style.Triggers>
                    <DataTrigger Value="True">
                        <DataTrigger.Binding>
                            <MultiBinding Converter="{StaticResource ImageActiveConverter}">
                                <Binding />
                                <Binding Path="DataContext.Selected" RelativeSource="{RelativeSource AncestorType={x:Type TreeViewItem}, AncestorLevel=3}" />
                            </MultiBinding>
                        </DataTrigger.Binding>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <ContextMenu x:Key="ImageContextMenu">
                <MenuItem Header="Add" Click="AddImage"/>
                <MenuItem Header="Remove" Click="Remove"/>
            </ContextMenu>

            <ContextMenu x:Key="LayerContextMenu">
                <MenuItem Header="Add">
                    <MenuItem Header="Vector" Click="AddVectorLayer"/>
                    <MenuItem Header="Hybrid" Click="AddHybridLayer"/>
                    <MenuItem Header="Raster" Click="AddRasterLayer"/>
                </MenuItem>
                <MenuItem Header="Edit">
                    <MenuItem Header="Cut" Click="Cut"/>
                    <MenuItem Header="Copy" Click="Copy"/>
                    <MenuItem Header="Paste" Click="Paste"/>
                    <Separator/>
                    <MenuItem Header="Flatten" Click="Flatten"/>
                </MenuItem>
                <MenuItem Header="Remove" Click="Remove"/>
            </ContextMenu>

            <ContextMenu x:Key="RenderableContextMenu">
                <MenuItem Header="Edit">
                    <MenuItem Header="Cut" Click="Cut"/>
                    <MenuItem Header="Copy" Click="Copy"/>
                    <MenuItem Header="Paste" Click="Paste"/>
                </MenuItem>
                <MenuItem Header="Remove" Click="Remove"/>
            </ContextMenu>

            <Style x:Key="VisibilityToggleStyle" TargetType="ToggleButton" x:Shared="False">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border BorderThickness="1" BorderBrush="Transparent" Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <fa:IconBlock Icon="Eye" Foreground="Gray" FontSize="{TemplateBinding FontSize}"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <fa:IconBlock Icon="EyeSlash" Foreground="Gray" FontSize="{TemplateBinding FontSize}"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="{x:Null}">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <fa:IconBlock Icon="Eye" Foreground="Gray" FontSize="{TemplateBinding FontSize}"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                        <Condition Property="IsChecked" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <fa:IconBlock Icon="EyeSlash" Foreground="Black" FontSize="{TemplateBinding FontSize}"/>
                                        </Setter.Value>
                                    </Setter>
                                </MultiTrigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                        <Condition Property="IsChecked" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <fa:IconBlock Icon="Eye" Foreground="Black" FontSize="{TemplateBinding FontSize}"/>
                                        </Setter.Value>
                                    </Setter>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <DataTemplate x:Key="ImageTitleTemplate">
                <Grid>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Center" Margin="5,0,0,0" MinWidth="60" LostFocus="OnAddComplete" KeyDown="OnAdd" Loaded="OnAddStarted"/>
                    <TextBlock Text="{Binding Title, Mode=OneWay}" VerticalAlignment="Center" Margin="5,0,0,0" Grid.Column="1">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource HoverStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <MultiBinding Converter="{StaticResource ImageActiveConverter}">
                                                <Binding />
                                                <Binding Path="DataContext.ActiveImage" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                            </MultiBinding>
                                        </DataTrigger.Binding>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </DataTemplate>

            <DataTemplate x:Key="LayerTitleTemplate">
                <Grid>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}" VerticalAlignment="Center" Margin="5,0,0,0" MinWidth="60" LostFocus="OnAddComplete" KeyDown="OnAdd" Loaded="OnAddStarted"/>
                    <TextBlock Text="{Binding Title, Mode=OneWay}" VerticalAlignment="Center"  Margin="5,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource HoverStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Value="True">
                                        <DataTrigger.Binding>
                                            <MultiBinding Converter="{StaticResource LayerActiveConverter}">
                                                <Binding />
                                                <Binding Path="DataContext.ActiveLayer" RelativeSource="{RelativeSource AncestorType={x:Type TreeViewItem}, AncestorLevel=2}" />
                                                <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType={x:Type TreeViewItem}, AncestorLevel=2}" />
                                                <Binding Path="DataContext.ActiveImage" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                            </MultiBinding>
                                        </DataTrigger.Binding>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </DataTemplate>

            <HierarchicalDataTemplate x:Key="ProjectTreeTemplate" ItemsSource="{Binding Layers}" DataType="{x:Type structure:XImage}">
                <DockPanel x:Name="ImageNode" LastChildFill="True" Margin="0,5,0,2" ContextMenu="{StaticResource ImageContextMenu}" MouseDown="SelectNode">
                    <fa:IconBlock Icon="Image" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource ImageTitleTemplate}"/>
                </DockPanel>
                <HierarchicalDataTemplate.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Shapes}" DataType="{x:Type structure:XLayer}">
                        <DockPanel LastChildFill="True" Margin="0,2" ContextMenu="{StaticResource LayerContextMenu}" MouseDown="SelectNode">
                            <ToggleButton IsChecked="{Binding IsVisible}" VerticalAlignment="Center" Background="Transparent" BorderBrush="Transparent" FontSize="12" Style="{StaticResource VisibilityToggleStyle}" DockPanel.Dock="Right" Margin="5,0"/>
                            <ContentControl Content="{Binding}" VerticalAlignment="Center">
                                <ContentControl.Resources>
                                    <DataTemplate DataType="{x:Type structure:XVectorLayer}">
                                        <fa:IconBlock Icon="V" Margin="0,2,0,0"/>
                                    </DataTemplate>
                                    <DataTemplate DataType="{x:Type structure:XRasterLayer}">
                                        <fa:IconBlock Icon="R" Margin="0,2,0,0"/>
                                    </DataTemplate>
                                    <DataTemplate DataType="{x:Type structure:XHybridLayer}">
                                        <fa:IconBlock Icon="H" Margin="0,2,0,0"/>
                                    </DataTemplate>
                                </ContentControl.Resources>
                            </ContentControl>
                            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource LayerTitleTemplate}"/>
                        </DockPanel>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate DataType="{x:Type structure:XRenderable}">
                                <StackPanel Orientation="Horizontal" ContextMenu="{StaticResource RenderableContextMenu}" MouseDown="SelectNode" Margin="0,1">
                                    <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource ShapeSelector}" VerticalAlignment="Center" FontSize="10" Width="18" Height="18"/>
                                    <TextBlock Text="{Binding Type, Mode=OneWay}" VerticalAlignment="Center" Margin="5,0,0,0" Style="{StaticResource ActiveHoverStyle}"/>
                                    <TextBlock Text="{Binding Style, Mode=OneWay, StringFormat=[{0}]}" VerticalAlignment="Center" Margin="3,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </HierarchicalDataTemplate.ItemTemplate>
            </HierarchicalDataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch">
        <ScrollViewer MouseDown="Unselect" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" DataContext="{Binding Project}">
            <TreeView x:Name="ImageTree" ItemsSource="{Binding Images}" ItemTemplate="{StaticResource ProjectTreeTemplate}" BorderBrush="Transparent" Margin="0,-1,0,0" PreviewMouseLeftButtonDown="TreeView_PreviewMouseLeftButtonDown" MouseMove="TreeView_MouseMove" DragEnter="TreeView_DragOver" Drop="TreeView_Drop" AllowDrop="True" ItemContainerStyle="{StaticResource StretchingTreeView}">
                <TreeView.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black" />
                </TreeView.Resources>
            </TreeView>
        </ScrollViewer>
    </Grid>
</UserControl>
