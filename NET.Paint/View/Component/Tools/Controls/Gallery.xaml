<UserControl x:Class="NET.Paint.View.Component.Tools.Controls.Gallery"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NET.Paint.View.Component.Tools.Controls"
             xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:service="clr-namespace:NET.Paint.Drawing.Service;assembly=NET.Paint.Drawing"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200" d:Background="White"
             d:DataContext="{d:DesignInstance Type={x:Type service:DesktopViewModel}, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <Style x:Key="ButtonRoundedCorner" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5" Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="LightGray">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="Gray"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                            </ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <DataTemplate x:Key="AddBitmapButton">
            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="2" CornerRadius="3">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="Gray"/>
                </Border.Effect>
                <Button Click="AddBitmap" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel>
                        <icons:MaterialIcon Kind="ImageAddOutline" Width="73" Height="60" Margin="2" Foreground="Gray"/>
                        <TextBlock Text="Add Bitmap" FontSize="10" Foreground="Gray" HorizontalAlignment="Center" Margin="2,0"/>
                    </StackPanel>
                </Button>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <StackPanel>
        <Label Content="Gallery:" Padding="0" Margin="2,2,0,2" DockPanel.Dock="Left" VerticalAlignment="Center"/>
        <Border BorderBrush="LightGray" BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" Height="300" VerticalScrollBarVisibility="Auto" Background="#F8F8F8">
                <ItemsControl>
                    <ItemsControl.Resources>
                        <CollectionViewSource x:Key="BitmapCollection" Source="{Binding Project.Bitmaps}"/>
                    </ItemsControl.Resources>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemsSource>
                        <CompositeCollection>
                            <CollectionContainer Collection="{Binding Source={StaticResource BitmapCollection}}"/>
                            <ContentPresenter ContentTemplate="{StaticResource AddBitmapButton}"/>
                        </CompositeCollection>
                    </ItemsControl.ItemsSource>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="2" CornerRadius="3">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="Gray"/>
                                </Border.Effect>
                                <Button Click="SelectBitmap" Padding="3" Background="Transparent"  BorderThickness="0" Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E3F2FD"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <StackPanel>
                                        <Image Source="{Binding}" Width="80" Height="60" Stretch="Uniform" Margin="2"/>
                                        <TextBlock FontSize="10" Foreground="Gray" HorizontalAlignment="Center" Margin="2,0">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0}×{1}">
                                                    <Binding Path="PixelWidth"/>
                                                    <Binding Path="PixelHeight"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                </Button>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </StackPanel>
</UserControl>
