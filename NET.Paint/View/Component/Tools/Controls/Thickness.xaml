<UserControl x:Class="NET.Paint.View.Component.Tools.Controls.Thickness"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="250" d:Background="White">
    <UserControl.Resources>
        <ResourceDictionary Source="../../../../Resources/Styles/Sliders.xaml"/>
    </UserControl.Resources>
    <Grid Height="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Slider TickFrequency="0.1" Minimum="0.1" Maximum="20" Value="{Binding StrokeThickness, Mode=TwoWay}" IsSnapToTickEnabled="True" Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource TriangularSliderStyle}" Height="20" IsMoveToPointEnabled="True"/>
        <TextBlock Text="{Binding StrokeThickness, StringFormat=N1}" Grid.Column="1" FontSize="13" VerticalAlignment="Center" Margin="5,0,0,0"/>
    </Grid>
</UserControl>
