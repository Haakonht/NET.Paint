<UserControl x:Class="NET.Paint.View.Component.Tools.Subcomponent.General"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NET.Paint.View.Component.Tools.Subcomponent"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:options="clr-namespace:NET.Paint.Drawing.Constant;assembly=NET.Paint.Drawing"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" DataContext="{Binding Tools}" Margin="5" x:Name="GeneralGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <xctk:ColorPicker SelectedColor="{Binding StrokeColor}" ShowDropDownButton="False" Grid.Row="0" Background="{Binding StrokeColor, Converter={StaticResource ColorToBrushConverter}}" VerticalContentAlignment="Stretch"/>
        <xctk:ColorPicker SelectedColor="{Binding FillColor}" ShowDropDownButton="False" Grid.Row="0" Background="{Binding FillColor, Converter={StaticResource ColorToBrushConverter}}" VerticalContentAlignment="Stretch" Margin="5"/>
        <StackPanel Grid.Row="1">
            <Label Content="Thickness:" Padding="0" Margin="2,0,0,2" FontSize="10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <Slider Value="{Binding StrokeThickness}" TickFrequency="0.1" Minimum="0.1" Maximum="20"/>
                <TextBlock Grid.Column="1" Text="{Binding StrokeThickness, StringFormat='{}{0:F1}'}" VerticalAlignment="Center" Margin="5,0,0,0"/>
            </Grid>
            <Label Content="Style:" Padding="0" Margin="2,2,0,2" FontSize="10"/>
            <ComboBox ItemsSource="{x:Static options:XConstants.StrokeStyleOptions}" SelectedItem="{Binding StrokeStyle}" Height="25">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="2" Height="20">
                            <Line X1="5" Y1="10" X2="85" Y2="10" Stroke="Black" StrokeThickness="2" Margin="0,0,20,0">
                                <Line.StrokeDashArray>
                                    <Binding Path="DashArray" />
                                </Line.StrokeDashArray>
                            </Line>
                            <TextBlock Text="{Binding Name}" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
