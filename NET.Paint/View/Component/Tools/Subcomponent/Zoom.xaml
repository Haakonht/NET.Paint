<UserControl x:Class="NET.Paint.View.Component.Tools.Subcomponent.Zoom"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="250" d:Background="White">
    <StackPanel>
        <Slider Minimum="0.1" Maximum="2.0" Value="{Binding Zoom, Mode=TwoWay}" TickFrequency="0.1"/>
        <Grid Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="{fa:Icon MagnifyingGlassMinus}" Grid.Column="0" Height="25" Click="ZoomOut"/>
            <Button Content="Reset" Grid.Column="1" Height="25" Click="ResetZoom"/>
            <Button Content="{fa:Icon MagnifyingGlassPlus}" Grid.Column="2" Height="25" Click="ZoomIn"/>
        </Grid>
    </StackPanel>
</UserControl>
