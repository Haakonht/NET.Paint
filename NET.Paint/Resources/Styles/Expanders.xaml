<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SimpleExpander" TargetType="Expander">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="Transparent">
                        <StackPanel>
                            <ToggleButton x:Name="HeaderToggleButton" Content="{TemplateBinding Header}" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" Background="DimGray" BorderThickness="0" Padding="5" HorizontalContentAlignment="Left" Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <DockPanel>
                                            <Path x:Name="Caret" Width="12" Height="6" Stretch="Uniform" Stroke="Black" Fill="Black" Margin="0,0,5,0" StrokeThickness="2" VerticalAlignment="Center" Data="M 0 3 L 12 3" />
                                            <ContentPresenter VerticalAlignment="Center" />
                                        </DockPanel>

                                        <ControlTemplate.Triggers>
                                            <!-- Down triangle -->
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Caret" Property="Data" Value="M 0 0 L 12 0 L 6 6 Z" />
                                            </Trigger>
                                            <!-- Horizontal line -->
                                            <Trigger Property="IsChecked" Value="False">
                                                <Setter TargetName="Caret" Property="Data" Value="M 0 3 L 12 3" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="FontWeight" Value="Normal"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite" ContentSource="Content" Visibility="Collapsed" Margin="0,5,0,0" DataContext="{TemplateBinding DataContext}" />
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NoCaretExpander" TargetType="Expander">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                        <DockPanel>
                            <!-- Header: Use ContentPresenter so you can put any control, e.g. ToggleButton -->
                            <ContentPresenter
                            ContentSource="Header"
                            RecognizesAccessKey="True"
                            Margin="0"
                            DockPanel.Dock="Top"/>
                            <!-- Content -->
                            <ContentPresenter
                            x:Name="ExpandSite"
                            Margin="{TemplateBinding Padding}"
                            Visibility="Collapsed"
                            DockPanel.Dock="Bottom"/>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Custom Expander Style -->
    <Style x:Key="CategoryExpanderStyle" TargetType="Expander">
        <Setter Property="IsExpanded" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}">
                        <DockPanel>
                            <!-- Custom Header -->
                            <ToggleButton x:Name="HeaderToggle" DockPanel.Dock="Top"  Content="{Binding CategoryName}" Foreground="White" FontWeight="SemiBold"
                                            IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource AncestorType=Expander}}"
                                            Background="DarkGray" BorderBrush="Gray" BorderThickness="1" Padding="0">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" 
                                                  BorderBrush="{TemplateBinding BorderBrush}" 
                                                  BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Expand/Collapse Icon -->
                                                <TextBlock Grid.Column="0" Text="▶" FontSize="8" VerticalAlignment="Center" 
                                                             Margin="3,0,2,0" Foreground="White" RenderTransformOrigin="0.5,0.5">
                                                    <TextBlock.RenderTransform>
                                                        <RotateTransform x:Name="ExpandRotate" Angle="0"/>
                                                    </TextBlock.RenderTransform>
                                                </TextBlock>

                                                <!-- Category Name -->
                                                <ContentPresenter x:Name="HeaderContent" Grid.Column="1" 
                                                                    Content="{TemplateBinding Content}" 
                                                                    VerticalAlignment="Center" 
                                                                    HorizontalAlignment="Left"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="ExpandRotate" 
                                                                               Storyboard.TargetProperty="Angle" 
                                                                               To="90" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="ExpandRotate" 
                                                                               Storyboard.TargetProperty="Angle" 
                                                                               To="0" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Content Area -->
                            <ContentPresenter x:Name="ContentPresenter" DockPanel.Dock="Top"/>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>