<Application x:Class="NET.Paint.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NET.Paint"
             xmlns:converter="clr-namespace:NET.Paint.Converter"
             StartupUri="View/MainWindow.xaml">
    <Application.Resources>
        <converter:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        <converter:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
        <converter:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converter:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converter:ValueGreaterThanConverter x:Key="ValueGreaterThanConverter"/>
        <converter:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>

        <Style TargetType="TextBlock" x:Key="HoverStyle">
            <Setter Property="FontWeight" Value="Normal"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
